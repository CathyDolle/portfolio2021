const lightColors = {
  fontColor: 'black',
  bgColor: 'white',
  mainColor: '#ef4234',
  lineColor: '#171717',
}

const darkColors = {
  fontColor: 'white',
  mainColor: '#ef4234',
  bgColor: 'black',
  lineColor: '#171717',
}

const margins = {

}

const padding = {
  wrapperPadding: '150px',
}

const lightTheme = {
  colors: lightColors,
  margins: margins,
  padding: padding,
}

const darkTheme = {
  colors: darkColors,
  margins: margins,
  padding: padding,
}
  
export { lightTheme, darkTheme };